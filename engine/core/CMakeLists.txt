file(GLOB ENGINE_CORE_HEADER_FILES "include/*.h")
file(GLOB ENGINE_CORE_SOURCE_FILES "source/*.cc")
add_library(engine-core STATIC ${ENGINE_CORE_HEADER_FILES} ${ENGINE_CORE_SOURCE_FILES})
target_link_libraries(engine-core PRIVATE SDL3::SDL3)
target_include_directories(engine-core PUBLIC include)